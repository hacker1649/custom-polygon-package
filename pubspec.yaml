name: custom_polygon_package
description: A Flutter package for custom painting.
version: 0.0.1
homepage: https://github.com/hacker1649/custom-polygon-package

environment:
  sdk: '>=3.4.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
